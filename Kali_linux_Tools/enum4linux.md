

```
For find bassic cradensiuls and simple enumurations:-
	enum4linux -a <ip address>
```

```
Search smb servces:-

enum4linux -L -S <target-ip>

-L --> list available user on the target system
-S --> Enumerates shared printers on the target system
```
